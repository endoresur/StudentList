import styled from "styled-components"

export const FilterRoot = styled.div`
    width: 100%;
    height: auto;

    margin-bottom: 32px;

    position: relative;
    display: flex;

    justify-content: space-between;
    align-items: center;
    flex-direction: row;

`